<div class="panel-heading">
	<p class="panel-title">Curriculums recibidas
		<span class="pull-right">
		</span>
	</p>
</div>
<div class="panel-body">
	<table class="table" >
		<thead class="bg-primary">
			<tr>
				<th>Contact</th>
				<th>Contact tel.</th>
				<th>Contact email</th>
				<th>Date sent</th>
				<th>CV</th>
			</tr>
		</thead>
		{% for row in data %}
			{% include 'adminarea/partials/curriculums_row.twig' with {'row':row} %}
		{% endfor %}
	</table>
</div>

<div class="panel-footer">
	{% include 'adminarea/partials/pagination_control.twig' with {'count':count, 'page': page, 'pageType' : 'curriculums'} %}
</div>